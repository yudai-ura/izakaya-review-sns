<%= link_to '一覧に戻る', reviews_path %>

<div class='container'>
    
 
    <div>
      <div style='font-size: 50px; margin-top: 20px;'>
        <% if @user.myimage? %>
          <%= image_tag @user.myimage.thumb100.url, class: 'myimage'  %>
        <% end %>
    
        <span>  <%= @user.name %>  さんのページ</span>
       </div>
       
       
       <div class='profile-btn'>
           <% if current_user == @user %>
            <%= link_to 'プロフィール編集', edit_user_path(@user), class: 'btn btn-primary' %>
            <%= link_to '新規投稿', new_review_path, class: 'btn btn-primary' %>
          
          <% else %>
            <%= render 'relationships/follow_button', user: @user %>
          <% end %>
        
          
       </div>
       
       <p>プロフィール文</p>
       <div class='user-profile'><%= simple_format(@user.profile) %></div>
      
    
  </div>
  
  <div class="container">
    <ul class="nav nav-tabs nav-justified mb-3">
      
      <li class="nav-item"><a href="<%= user_path(@user) %>" class="nav-link <%= 'active' if current_page?(user_path(@user)) %>">レビュー <span class="badge badge-secondary"><%= @count_reviews %></span></a></li>
      <li class="nav-item"><a href="<%= followings_user_path(@user) %>" class="nav-link <%= 'active' if current_page?(followings_user_path(@user)) %>">フォロー  <span class="badge badge-secondary"><%= @count_followings %></span></a></li>
      <li class="nav-item"><a href="<%= followers_user_path(@user) %>" class="nav-link <%= 'active' if current_page?(followers_user_path(@user)) %>">フォロワー <span class="badge badge-secondary"><%= @count_followers %></span></a></li>
      <li class="nav-item"><a href="<%= likes_user_path(@user) %>" class="nav-link <%= 'active' if current_page?(likes_user_path(@user)) %>">お気に入り <span class="badge badge-secondary"><%= @count_favoreview %></span></a></li>
    </ul>
    
   <%= render 'reviews/reviews', reviews: @reviews %>
  </div>
    
</div>